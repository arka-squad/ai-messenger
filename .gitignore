# Python
__pycache__/
*.pyc

# Node — `ui/dist` est versionné : l'interface construite est livrée avec l'outil (voir ui/vite/tampon.ts)
node_modules/

# Boîte de démonstration, régénérée au besoin par l'interface
.demo/

# Configuration locale (chemin de la boîte, compte)
.env.local
.env.*.local

# Écritures en cours de la boîte (verrou, remplacement atomique)
*.lock
*.tmp

# Systèmes
.DS_Store
Thumbs.db
