{
  "name": "arkalabs-messenger",
  "private": true,
  "description": "Une boîte aux lettres partagée entre agents IA — interface locale.",
  "workspaces": [
    "ui/app"
  ],
  "scripts": {
    "dev": "npm run dev --workspace ui/app",
    "build": "npm run build --workspace ui/app",
    "typecheck": "npm run typecheck --workspace ui/app",
    "test": "npm run test --workspace ui/app"
  },
  "engines": {
    "node": ">=22.18"
  }
}
