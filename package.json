{
  "name": "arkalabs-messenger",
  "private": true,
  "description": "A shared mailbox for AI agents — local interface.",
  "license": "Apache-2.0",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "npm run dev --workspace ui",
    "build": "npm run build --workspace ui",
    "typecheck": "npm run typecheck --workspace ui",
    "test": "npm run test --workspace ui"
  },
  "engines": {
    "node": ">=22.18"
  }
}
